services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ftrepo
      POSTGRES_PASSWORD: ftrepo
      POSTGRES_DB: ftrepo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ftrepo"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    image: ghcr.io/ftrepoxyz/ftrepo-app:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://ftrepo:ftrepo@db:5432/ftrepo?schema=public
      # All other settings (GitHub, Telegram, intervals, etc.) are now
      # configured via the Settings UI and stored in the database.
    depends_on:
      db:
        condition: service_healthy

  worker:
    image: ghcr.io/ftrepoxyz/ftrepo-worker:latest
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://ftrepo:ftrepo@db:5432/ftrepo?schema=public
      # All other settings (GitHub, Telegram, intervals, etc.) are now
      # configured via the Settings UI and stored in the database.
    volumes:
      - tdlib-data:/app/tdlib-data
      - temp-downloads:/tmp/ftrepo
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
  tdlib-data:
  temp-downloads:
